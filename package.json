{
  "name": "project-whiteboard",
  "version": "1.0.0",
  "description": "Project management app",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --asar --out=dist/ --overwrite --ignore=.*\\.sh --ignore=\\.gitignore --ignore=README\\.md"
  },
  "dependencies": {
    "archiver": "^3.0.0",
    "jquery": "^3.5.0",
    "tmp": "^0.1.0"
  },
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-packager": "^14.0.0"
  }
}
